# Swift Package Manager
.build/
.swiftpm/

# Xcode derived data and user state
DerivedData/
xcuserdata/
*.xcuserstate

# Generated by xcodegen from project.yml — regenerate with `xcodegen generate`, don't hand-edit
WildPairs.xcodeproj/

# macOS metadata
.DS_Store

# Logs and credentials
*.log
.env
*.pem
*.p12
*.mobileprovision

# OneDrive conflict copies
*conflicted copy*
*Conflict*
*.moved-aside

# Phase 17 raw card-art source pack (192 MB) — kept on disk for Stage 5 extraction,
# too large for git history. Downscaled @3x assets are added to the app target instead.
docs/solo_swiftui_asset_pack.zip
